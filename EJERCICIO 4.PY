# Solicita el valor de N al usuario
N = int(input("Ingrese un valor entero positivo: "))

# Calcula la suma de los N primeros enteros positivos
suma = (N * (N + 1)) / 2

# Muestra la suma
print("La suma de los N primeros enteros positivos es:", suma)